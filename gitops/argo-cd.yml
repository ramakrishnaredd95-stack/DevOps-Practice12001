apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: boutique
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/ramakrishnaredd95-stack/DevOps-Practice12001.git'
    targetRevision: project-demo
    path: gitops
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: boutique
  # syncPolicy: {}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
